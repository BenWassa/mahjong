import raw from "../../../docs/HKOS_RULES.md?raw";

/**
 * The exact, bundled text of docs/HKOS_RULES.md. Imported rather than
 * transcribed so the in-app rules reference can never drift from the
 * authoritative rules contract: a change there ships here automatically.
 */
export const HKOS_RULES_MARKDOWN: string = raw;
